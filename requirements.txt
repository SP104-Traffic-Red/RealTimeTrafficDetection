# SP-104 Red: Traffic Detection Dependencies
# NOTE: Install PyTorch with CUDA separately FIRST (see README)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.0.0
opencv-python>=4.8.0
numpy
